# Star-Pattern-Print
⭐ Right-Angled Triangle Pattern in Python

This project is a simple Python program that prints a right-angled triangle pattern using asterisks (*). The number of rows in the triangle is determined by user input.

The program demonstrates how patterns can be generated using loops and string operations in Python.
